{
  "name": "trek picker codespace",
  "image": "mcr.microsoft.com/devcontainers/javascript-node:20-bookworm",
  "customizations": {
    "vscode": {
      "extensions": [
        // "GitHub.copilot",
        "mgmcdermott.vscode-language-babel"
      ]
    }
  },
  "features": {
    "ghcr.io/devcontainers/features/docker-in-docker:2": {}
  },
  "postCreateCommand": "cd client && npm install && cd ../server && npm install && cp .env.example .env && npx prisma generate"
}
