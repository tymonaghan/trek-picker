{
  "version": "0.2.0",
  "configurations": [
    {
      "command": "npm start",
      "name": "run npm start",
      "request": "launch",
      "type": "node-terminal"
    },
    {
      "type": "chrome",
      "request": "launch",
      "name": "launch in chrome",
      "url": "http://localhost:8080",
      "webRoot": "${workspaceFolder}"
    }
  ]
}
